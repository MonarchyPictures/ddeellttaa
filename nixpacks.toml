# Railway Nixpacks Configuration for Delta 9
[phases.build]
cmds = ["pip install -r requirements.txt"]

[phases.setup]
nixPkgs = ["python311", "gcc"]

[start]
cmd = "uvicorn app_dashboard:app --host 0.0.0.0 --port $PORT"
